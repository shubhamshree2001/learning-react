/* here we will first import and then from here only we will export 
suppose we have created search component that also we can add here and export 
now anywhere if we use that component just we need to import to index.js file*/
import InputBox from "./InputBox";
export { InputBox }; 